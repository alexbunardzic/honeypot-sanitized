{
  "name": "honeypot-sanitized",
  "version": "1.0.0",
  "description": "AI Craftspeople Guild first mob programming session",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "mutate": "stryker run"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^8.7.1",
    "@stryker-mutator/jest-runner": "^8.7.1",
    "jest": "^29.7.0"
  }
}
